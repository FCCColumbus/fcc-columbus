import MembersContainer from '../MembersContainer';

describe('MembersContainer', () => {
  it('should render', () => {
    const instance = new MembersContainer();

    expect(instance).toBeTruthy();
  });
});

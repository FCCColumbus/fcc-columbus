import CalendarContainer from '../CalendarContainer'

describe('CalendarContainer', () => {
  it('should render', () => {
    const instance = CalendarContainer()

    expect(instance).toBeTruthy()
  })
})

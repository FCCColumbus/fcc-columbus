import AdminsContainer from '../AdminsContainer'

describe('AdminsContainer', () => {
  it('should render', () => {
    const instance = new AdminsContainer()
    expect(instance).toBeTruthy()
  })
})
